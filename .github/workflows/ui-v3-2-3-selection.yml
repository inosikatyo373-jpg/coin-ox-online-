name: Apply BID GRID v3.2.3 selection clarity
on:
  push:
    paths:
      - '.github/workflows/ui-v3-2-3-selection.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch UI
        run: python .github/scripts/patch_v3_2_3.py
      - name: Validate JavaScript
        run: node --check /tmp/bid-grid-inline.js
      - name: Commit changes
        run: |
          rm -f .github/workflows/ui-v3-2-3-selection.yml .github/scripts/patch_v3_2_3.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/index.html .github/workflows/ui-v3-2-3-selection.yml .github/scripts/patch_v3_2_3.py
          git commit -m "Improve BID GRID v3.2.3 selected cell clarity"
          git push
